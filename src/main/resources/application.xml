<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context-4.0.xsd">

    <!--    The first way to initialize a bean-->
    <!--  <bean class="org.springframework.context.annotation.CommonAnnotationBeanPostProcessor"/>     Needed for bean definition creation with [annotation] -->

    <!--    The second way to initialize a bean (better) -->
    <context:annotation-config/>

    <!--    The first way to configure [properties]-->

    <!--    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer">-->
    <!--        <property name="location" value="classpath:application.properties"/>-->
    <!--    </bean>-->

    <!--    The second way to configure [properties] (better)-->

    <!--    Add to the head: xmlns:context="http://www.springframework.org/schema/context"-->
    <context:property-placeholder location="classpath:application.properties"/>

    <bean class="spring.bpp.InjectBeanPostProcessor" />

    <bean id="repo1" name="r1, r2" class="spring.database.repository.UserRepository"/>
    <!--    Bean definition created with constructor-->

    <bean class="spring.database.repository.pool.ConnectionPool">
        <constructor-arg name="username" value="${db.username}"/>
        <constructor-arg name="password" value="${db.password}"/>
        <constructor-arg name="poolSize" value="${db.poolsize}"/>
        <constructor-arg name="url" value="${db.url}"/>
    </bean>
    

    <!--    Bean we use as reference in other beans-->
    <bean name="driver" class="java.lang.String">
        <constructor-arg value="PostgresDriver"/>
    </bean>


    <!--    Task: create Bean UserService-->
    <!--    First we create bean UserDto-->
    <bean name="userDto" class="spring.dto.UserDto" scope="prototype"/>  <!--  [scope]="prototype" - new bean for each call -->

    <!--    Then we need a UserMapper bean-->
    <bean name="userMapper" class="spring.mapper.UserMapper">
        <constructor-arg ref="userDto"/>
<!--        <property name="userDto" ref="userDto" />   why doesn't it work like this? -->
    </bean>
    <!--    And now we can create a UserService bean-->
    <bean name="userService" class="spring.service.UserService">
         <constructor-arg ref="userMapper"/>
         <constructor-arg ref="r1"/>
<!--        <property name="userMapper" ref="userMapper" />-->
<!--        <property name="userRepository" ref="r1" />-->
    </bean>

<!--    Bean definition created with setters-->

<!--    <bean id="repo2" name="r3" class="spring.database.repository.UserRepository">-->
<!--        <property name="userName" ref="driver" />  &lt;!&ndash;    ref="driver" instead of value="user2" to make dependency injection possible&ndash;&gt;-->
<!--        <property name="properties" >-->
<!--            <map>-->
<!--                <entry key="user" value="Sofi" />-->
<!--                <entry key="address" value-ref="driver" />  &lt;!&ndash;    key-ref/value-ref="driver" instead of key/value="Berlin" if we want to use another bean &ndash;&gt;-->
<!--            </map>-->
<!--        </property>-->
<!--    </bean>-->

</beans>